---
type: blueprint
title: "Correct setup-wp current information"
status: __wp-lifecycle-status__
complexity: S
owner: "webpresso/github-actions"
created: "2026-08-23"
---

# Correct setup-wp current information

## Product wedge anchor

- **Stage outcome:** Consumers can distinguish the setup action's current public download behavior from future qualification and updater work
- **Consuming surface:** README setup-wp and freshness guidance plus focused source-parity tests
- **New user-visible capability:** Truthful source, assets, token, version, checksum, cache, package-root, and freshness semantics

## Summary

Correct only stale README/freshness information against executable truth at `36b412a`. The action downloads exact-semver assets directly and without a token from configurable public `webpresso/app-releases`, optionally verifies a caller checksum, reuses/seeds a versioned tool-cache directory, and fetches `wp-package-root.tgz` from the same release with no tag-archive fallback. It does not resolve ranges/latest or self-update. Freshness remains a separate npm-migration scanner and deliberately does not rewrite setup-wp's product-version axis. Do not change action behavior, identifiers, pins, or future qualification scope.

#### Task 1.1: Correct README and freshness truth without behavior changes

**Status:** __wp-task-status__
**Wave:** 1
**Lane:** truth
**Depends:** None
**Produces:** actions-current-truth-merge-sha

Write a focused source-parity test red first for the stale README lines. Update README text only where it contradicts action/workflow source: remove the obsolete `github-token` claim; describe public configurable `webpresso/app-releases`, tokenless direct URLs, exact product semver independent of action SHA, four exact binary names, optional checksum, cache hit/best-effort seed, same-release `wp-package-root.tgz`, current package-root exports and fail-closed missing-catalog behavior, and no source/tag-archive fallback. Correct the freshness workflow filename/name and three npm pin shapes; state it deliberately excludes setup-wp and is not updater behavior. Existing action/workflow comments that already match source stay unchanged. Add no future support claim.

**Acceptance:**
- [_] Focused test reads executable action/workflow plus README and fails before the prose correction
- [_] README names configurable public `webpresso/app-releases`, tokenless direct URLs, and exact semver independent from action SHA
- [_] README pins `wp-linux-x64`, `wp-linux-arm64`, `wp-darwin-x64`, `wp-darwin-arm64`, and same-release `wp-package-root.tgz` with no tag-archive fallback
- [_] README states optional sha256 verification, version-directory tool-cache hit/best-effort seed, and that cache hits avoid download; it does not claim rehash-on-hit
- [_] README names `WEBPRESSO_PACKAGE_ROOT` plus legacy `WEBPRESSO_AGENT_KIT_ROOT` and fail-closed missing catalog
- [_] README rejects stale `github-token`, private webpresso/app, agent-kit-package.tgz, range/latest, self-update, and downloads-every-invocation claims
- [_] Freshness guidance names `.github/workflows/webpresso-freshness.yml`, `Reusable webpresso freshness`, job `webpresso-freshness`, three npm pin shapes, and explicit setup-wp exclusion
- [_] Diff is README, focused test, and blueprint evidence only; no action/workflow executable, external identifier, input, pin, URL, asset, permission, trigger, cache, checksum, or download behavior changes
- [_] `wp test --files test/setup-wp-current-information.test.ts` and existing setup-wp/freshness/workflow suites pass; controller records merge SHA

## Trust Dossier

### Readiness Verdict

- promotion-ready: true
- unresolved-count: 0
- trust-gate-version: v1

### Material Claims

| ID | Claim | Evidence |
| --- | --- | --- |
| C1 | setup-wp currently uses configurable public webpresso/app-releases and tokenless direct release URLs; github-token is not an input. | repo:.github/actions/setup-wp/action.yml |
| C2 | The action accepts exact semver only and selects four exact platform assets plus same-release wp-package-root.tgz with no source archive fallback. | repo:test/setup-wp.test.ts |
| C3 | Optional checksum verification and versioned tool-cache hit/best-effort seed are executable current behavior. | repo:.github/actions/setup-wp/action.yml |
| C4 | Freshness scans three npm pin shapes, is named webpresso-freshness, and deliberately excludes setup-wp's product axis. | repo:.github/workflows/webpresso-freshness.yml |
| C5 | README still names removed github-token/private package-root behavior and the retired freshness filename/four-shape scope. | repo:README.md |
| C6 | Existing suites lock current action and freshness executable behavior. | repo:test/freshness.test.ts |

### Material Decisions

| ID | Decision | Chosen option | Rejected alternatives | Rationale |
| --- | --- | --- | --- | --- |
| D1 | Authority after concurrent source advance | Re-plan from executable action/tests at exact current main | Preserve stale approved literals; roll back action behavior; blend future qualification claims | The prior review was evidence-bound to an older commit. Truth documentation must yield to current executable source without behavior rollback. |
| D2 | Change scope | README plus focused parity test only | Modify action/workflow; duplicate all executable tests; implement updater/qualification | The known inconsistencies are prose. Existing action/workflow behavior is already tested and out-of-scope future work remains separately sequenced. |
| D3 | Regression style | Assert semantic source-to-README parity and exact high-risk literals | Snapshot whole files; prose-only change without test; duplicate implementation | A focused contract catches reintroduced false claims while staying resilient to unrelated wording changes. |

### Promotion Gates

| Gate | Command | Expected outcome | __wp-gate-result__ | Defer |
| --- | --- | --- | __wp-gate-result__ | --- |
| Focused truth contract | wp test --files test/setup-wp-current-information.test.ts | README/action/freshness parity passes | __wp-gate-result__ | pre-implementation |
| README format | wp format --check --files README.md test/setup-wp-current-information.test.ts | Touched docs/test are formatted | __wp-gate-result__ | pre-implementation |

### Residual Unknowns

None.
