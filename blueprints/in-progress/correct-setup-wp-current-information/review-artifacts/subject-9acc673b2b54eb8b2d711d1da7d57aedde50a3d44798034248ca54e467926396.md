---
type: blueprint
title: Correct setup-wp current information
status: __wp-lifecycle-status__
complexity: M
owner: webpresso/github-actions
created: "2026-08-23"
---

# Correct setup-wp current information

## Product wedge anchor

- **Stage outcome:** Action consumers can distinguish current compatibility behavior from the future verified public installer target
- **Consuming surface:** github-actions README, setup-wp action descriptions/comments, freshness workflow comments, and contract tests
- **New user-visible capability:** Truthful version, source, credentials, asset, pin, and integrity semantics for setup-wp

## Summary

Correct current setup-wp information without changing behavior, public inputs, asset names, source URLs, workflow/job names, or pins. The `version` input is an exact wp product release version independent from the setup action SHA. The current action downloads private `webpresso/app` assets through `github-token`/GITHUB_TOKEN and legacy package-root names; `webpresso/app-releases` is a future source not supported today. The npm `@webpresso/agent-kit` freshness workflow is deprecated migration debt, and checksum, provenance, cache rehash, arbitrary-version, and self-update guarantees do not exist today.

#### Task 1.1: Correct setup-wp and freshness current truth

**Status:** __wp-task-status__
**Wave:** 1
**Lane:** truth
**Depends:** None
**Produces:** actions-current-truth-merge-sha

Write `test/setup-wp-current-information.test.ts` first and capture failure against the present wording. Then update only README plus descriptions/comments in `.github/actions/setup-wp/action.yml` and comments in `.github/workflows/agent-kit-freshness.yml`. Change no executable action/workflow behavior, public input key, asset name, source URL, workflow/job display name, version/action pin, cache behavior, trigger, permission, or reusable-workflow contract.

**Acceptance:**
- [_] The focused test inspects README.md and `.github/actions/setup-wp/action.yml`: both identify the current private `webpresso/app` exact-tag source and exact credential input `github-token`, whose runtime GITHUB_TOKEN must read private release assets; docs state the default `github.token` may need a caller-supplied token.
- [_] The focused test requires the `version` input to be an exact wp product semver independent from the immutable setup-wp action SHA and requires an affirmative sentence that it is not an `@webpresso/agent-kit` npm version.
- [_] The focused test inspects README.md and action descriptions/comments and requires an observable warning: `webpresso/app-releases` is not supported/configurable by this action today and must not be used until this action itself documents that source as supported.
- [_] The focused test requires positive limitation sentences in README/action comments stating that no checksum or digest verification, provenance verification, version ranges/latest selection, cache rehash, or wp self-update is performed today.
- [_] The focused test inspects `.github/workflows/agent-kit-freshness.yml` comments and README: both call npm `@webpresso/agent-kit` scanning deprecated migration debt and not a wp product updater; workflow and job `name:` values remain byte-identical.
- [_] The diff changes only prose, comments/descriptions, the focused test, and blueprint evidence. No YAML input key, workflow/job display name, executable shell/JavaScript, pin, URL, asset identifier, permission, trigger, dependency, or release behavior changes.
- [_] The focused contract passes through `wp test --files test/setup-wp-current-information.test.ts`; existing `test/freshness.test.ts` and `test/workflow-shape.test.ts` pass together under Bun as task evidence and required PR CI; the controller records the exact merge SHA.

## Trust Dossier

### Readiness Verdict

- promotion-ready: true
- unresolved-count: 0
- trust-gate-version: 1

### Material Claims

| ID | Claim | Evidence |
| --- | --- | --- |
| claim-1 | Current setup-wp input `github-token` maps to runtime GITHUB_TOKEN; that token must read exact-tag private webpresso/app release assets. | repo:.github/actions/setup-wp/action.yml |
| claim-2 | Current agent-kit freshness queries npm and is not a wp product updater. | repo:.github/workflows/agent-kit-freshness.yml |
| claim-3 | The existing freshness contract suite is at the exact path required as task and CI evidence. | repo:test/freshness.test.ts |
| claim-4 | The existing workflow-shape contract suite is at the exact path required as task and CI evidence. | repo:test/workflow-shape.test.ts |

### Material Decisions

| ID | Decision | Chosen option | Rejected alternatives | Rationale |
| --- | --- | --- | --- | --- |
| decision-1 | Do not disguise target-state action behavior as current. | Make a prose/comment/description-only correction, then repair downloads, integrity, and qualification in `qualify-setup-wp-against-release-a` under `release-install-consistency-program`. | Rename public inputs/assets/display names or change downloads, self-pins, cache, and public guidance in one unqualified PR. | Truth can land without compatibility risk; the named successor owns supply-chain behavior and live Release A evidence. |
| decision-2 | Keep the full Bun suites outside fixed-budget promotion commands. | Use one focused bounded promotion test and require the exact existing Bun suites as task evidence and PR CI. | Combine suites into one promotion command that risks the fixed 30-second per-command ceiling, convert the repo test framework, or omit integration proof. | The focused truth contract fits the gate budget; complete workflow suites remain mandatory in normal verification where the promotion deadline does not truncate them. |

### Promotion Gates

| Gate | Command | Expected outcome | __wp-gate-result__ | Defer |
| --- | --- | --- | __wp-gate-result__ | --- |
| docs-frontmatter | wp audit docs-frontmatter | Documentation surfaces contain no invalid frontmatter. | __wp-gate-result__ |  |
| truth-test | wp test --files test/setup-wp-current-information.test.ts | Focused file-level positive current-truth assertions pass after implementation. | __wp-gate-result__ | pre-implementation |

### Residual Unknowns

None.
