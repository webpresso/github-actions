---
type: blueprint
title: "Correct setup-wp current information"
status: __wp-lifecycle-status__
complexity: M
owner: "webpresso/github-actions"
created: "2026-08-23"
---

# Correct setup-wp current information

## Product wedge anchor

- **Stage outcome:** Action consumers can distinguish current compatibility behavior from the future verified public installer target
- **Consuming surface:** README, setup-wp descriptions/comments, freshness workflow comments, and contract tests
- **New user-visible capability:** Truthful version, source, credential, asset, pin, and integrity semantics for setup-wp

## Summary

Correct current setup-wp information without changing behavior or external identifiers. `version` is an exact wp product release version independent from the action SHA. The action downloads exact private `webpresso/app` tags through `github-token`, expects current platform wp binaries plus legacy optional `agent-kit-package.tgz`, and does not support `webpresso/app-releases` yet. npm `@webpresso/agent-kit` freshness is deprecated migration debt; integrity, range selection, cache rehash, and self-update guarantees do not exist today.

#### Task 1.1: Correct setup-wp and freshness current truth

**Status:** __wp-task-status__
**Wave:** 1
**Lane:** truth
**Depends:** None
**Produces:** actions-current-truth-merge-sha

Write `test/setup-wp-current-information.test.ts` first and capture failure. Then update only README, setup-wp descriptions/comments, and freshness workflow comments. Change no executable behavior, public input key, asset name, source URL, workflow/job name, pin, cache behavior, trigger, permission, or reusable-workflow contract.

**Acceptance:**
- [_] The focused test inspects README and setup-wp action text: both name current private `webpresso/app` exact tags, input `github-token` mapped to GITHUB_TOKEN, and the need for a token that can read private assets; contradictory public/anonymous-access claims are forbidden.
- [_] The focused test requires a positive statement that `version` is exact wp product semver independent from action SHA and not an `@webpresso/agent-kit` npm version, while rejecting contradictory npm-version/range/latest wording anywhere inspected.
- [_] The focused test requires README/action text to say `webpresso/app-releases` is unsupported and not configurable today until the action documents it as supported, and rejects any current-support claim.
- [_] The focused test pins current identifiers `wp-linux-x64`, `wp-linux-arm64`, `wp-darwin-arm64`, and optional legacy `agent-kit-package.tgz` (with tag-archive fallback) from action.yml; README/action text must not invent another current asset identifier.
- [_] The focused test requires positive statements that no checksum/digest, provenance, range/latest selection, cache rehash, or wp self-update occurs today and rejects contradictory guarantee claims anywhere inspected.
- [_] The focused test requires freshness comments/README to call npm agent-kit scanning deprecated migration debt, not a wp updater, and pins exact existing names `Reusable agent-kit freshness` and `agent-kit-freshness`; diff confirms workflow/job name lines untouched.
- [_] The diff changes only prose/comments/descriptions, focused test, and blueprint evidence. No input key, workflow/job name, executable shell/JS, pin, URL, asset identifier, permission, trigger, dependency, or release behavior changes.
- [_] The focused contract passes via `wp test --files test/setup-wp-current-information.test.ts`; exact existing freshness and workflow-shape Bun suites pass as task evidence/PR CI; controller records merge SHA.

## Trust Dossier

### Readiness Verdict

- promotion-ready: true
- unresolved-count: 0
- trust-gate-version: 1

### Material Claims

| ID | Claim | Evidence |
| --- | --- | --- |
| claim-1 | Input `github-token` maps to GITHUB_TOKEN and must read exact-tag private webpresso/app release assets. | repo:.github/actions/setup-wp/action.yml |
| claim-2 | Current setup-wp selects `wp-linux-x64`, `wp-linux-arm64`, or `wp-darwin-arm64`, optionally reads legacy `agent-kit-package.tgz`, and otherwise falls back to the tag archive. | repo:.github/actions/setup-wp/action.yml |
| claim-3 | Current agent-kit freshness queries npm and is not a wp product updater. | repo:.github/workflows/agent-kit-freshness.yml |
| claim-4 | The existing freshness suite path is exact. | repo:test/freshness.test.ts |
| claim-5 | The existing workflow-shape suite path is exact. | repo:test/workflow-shape.test.ts |

### Material Decisions

| ID | Decision | Chosen option | Rejected alternatives | Rationale |
| --- | --- | --- | --- | --- |
| decision-1 | Separate truth from target behavior. | Make prose/comment/description-only corrections; qualify downloads/integrity in `qualify-setup-wp-against-release-a`. | Rename public identifiers or change downloads/self-pins/cache now. | Truth lands without compatibility risk; the successor owns live Release A evidence. |
| decision-2 | Keep full Bun suites outside fixed-budget promotion commands. | One focused bounded gate plus exact existing suites as task evidence and PR CI. | Risk the fixed 30-second ceiling with combined suites, convert framework, or omit proof. | The focused contract fits the gate budget; full workflow suites remain mandatory under normal verification. |

### Promotion Gates

| Gate | Command | Expected outcome | __wp-gate-result__ | Defer |
| --- | --- | --- | __wp-gate-result__ | --- |
| docs-frontmatter | wp audit docs-frontmatter | Documentation frontmatter passes. | __wp-gate-result__ |  |
| truth-test | wp test --files test/setup-wp-current-information.test.ts | Focused positive and contradiction-rejecting truth assertions pass. | __wp-gate-result__ | pre-implementation |

### Residual Unknowns

None.
